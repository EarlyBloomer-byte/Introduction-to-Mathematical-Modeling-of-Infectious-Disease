Run `renv::init()` from the repository root in R. This will scan `DESCRIPTION`
and create `renv.lock`, pinning exact versions of ggplot2, dplyr, tidyr,
deSolve, pracma, plotly, latex2exp, rmarkdown, and knitr. `renv.lock` is
generated on first init and should be committed once created.
